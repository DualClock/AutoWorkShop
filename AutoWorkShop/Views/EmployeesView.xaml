<UserControl x:Class="AutoWorkshop.Views.EmployeesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="➕ Добавить сотрудника" Width="170" Height="35" Margin="0,0,10,0" 
                    Background="#007ACC" Foreground="White" Click="AddEmployee_Click"/>
            <Button Content="✏️ Редактировать" Width="120" Height="35" Margin="0,0,10,0" 
                    Background="#28A745" Foreground="White" Click="EditEmployee_Click"/>
            <Button Content="🗑️ Удалить" Width="100" Height="35" Margin="0,0,10,0" 
                    Background="#DC3545" Foreground="White" Click="DeleteEmployee_Click"/>
            <Button Content="🔄 Обновить" Width="100" Height="35" Click="Refresh_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="1" x:Name="EmployeesDataGrid" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="200"/>
                <DataGridTextColumn Header="Должность" Binding="{Binding Position}" Width="150"/>
                <DataGridTextColumn Header="Отдел" Binding="{Binding Department.Name}" Width="150"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="120"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>  