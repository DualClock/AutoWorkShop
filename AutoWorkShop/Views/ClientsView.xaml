<UserControl x:Class="AutoWorkshop.Views.ClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель управления -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="➕ Добавить клиента" Width="150" Height="35" Margin="0,0,10,0" 
                    Background="#007ACC" Foreground="White" Click="AddClient_Click"/>
            <Button Content="✏️ Редактировать" Width="120" Height="35" Margin="0,0,10,0" 
                    Background="#28A745" Foreground="White" Click="EditClient_Click"/>
            <Button Content="🗑️ Удалить" Width="100" Height="35" Margin="0,0,10,0" 
                    Background="#DC3545" Foreground="White" Click="DeleteClient_Click"/>
            <Button Content="🔄 Обновить" Width="100" Height="35" Click="Refresh_Click"/>
        </StackPanel>

        <!-- Таблица клиентов -->
        <DataGrid Grid.Row="1" x:Name="ClientsDataGrid" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="200"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="120"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                <DataGridTextColumn Header="Адрес" Binding="{Binding Address}" Width="*"/>
                <DataGridTextColumn Header="Дата регистрации" Binding="{Binding CreatedDate}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>